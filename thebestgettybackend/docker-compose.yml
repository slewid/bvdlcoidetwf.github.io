volumes:
  pet:

services:
  getty:
    image: getty:latest
    container_name: getty
    restart: always
    volumes:
      - type: volume
        source: pet
        target: /app/petcounter.txt
    ports:
      - "9999:9999"